# Vim stuffs
.vimrc.local

# Ruby stuffs
.patrol

# Rails stuffs
Gemfile.local*
Guardfile
config/environments/genki.rb

# Misc.
.envrc

# INJECT_START:"#{ENV['HOME']}/dotfiles/submodules/gitignore/Global/OSX.gitignore"
.DS_Store
.AppleDouble
.LSOverride
Icon


# Thumbnails
._*

# Files that might appear on external disk
.Spotlight-V100
.Trashes
# INJECT_END

# INJECT_START:"#{ENV['HOME']}/dotfiles/submodules/gitignore/Global/SASS.gitignore"
### SASS Ignores - "Sassy CSS" http://sass-lang.com/
*.sass-cache
# INJECT_END

# INJECT_START:"#{ENV['HOME']}/dotfiles/submodules/gitignore/Global/Tags.gitignore"
# Ignore tags created by etags and ctags
TAGS
!TAGS/
tags
!tags/
# INJECT_END

# INJECT_START:"#{ENV['HOME']}/dotfiles/submodules/gitignore/Global/vim.gitignore"
.*.s[a-w][a-z]
*.un~
Session.vim
.netrwhist
*~
# INJECT_END
