snippet     gtd
abbr        gets.to_i.times do...end
options     head
    gets.to_i.times do
      ${0}
    end

snippet     dir4
abbr        dir + dydx
options     head
    dir = [ :up, :right, :down, :left ]
    dy = { :up => -1, :right => 0, :down => 1, :left => 0 }
    dx = { :up => 0, :right => 1, :down => 0, :left => -1 }
