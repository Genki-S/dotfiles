---
- Slava/testerdream:
    build:
        mac: ant
        unix: ant

- visionmedia/git-extras:
    build:
        mac: make install
        unix: make install

- defunkt/dotjs:
    build:
        mac: rake install
        unix: rake install

- leftnode/get-shit-done:

- lpenz/atdtool:

- Thibaut/devdocs:
    build:
        mac: bundle install && thor docs:download --all
        unix: bundle install && thor docs:download --all
