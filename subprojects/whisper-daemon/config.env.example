# Whisper Daemon Configuration
# Copy this file to ~/.config/whisper-daemon/config.env and customize

# Microphone name (partial match, leave empty for default)
MIC_NAME=

# Server port
PORT=8000

# Pause time before considering speech ended (seconds)
PAUSE_TIME=1.5

# "Too quiet" threshold (higher = less sensitive)
HALLUCINATE_THRESHOLD=400

# Display for X11 (check with 'echo $DISPLAY')
DISPLAY=:1
