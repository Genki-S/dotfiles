# vim: filetype=zsh

# Some directories are often used.
# Therefore, expanding it with a few keystrokes is useful.
# This is inspired by vim's mark feature.

# TODO: move this hash to global
#       and make it modifiable with another zle widget
local REPLY
read-from-minibuffer -k 1 "Mark:"
LBUFFER+=${zvi_dirmarks[$REPLY]}
