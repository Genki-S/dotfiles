rcfiles=(
  prompt.zsh
  history.zsh
  zle.zsh
  git.zsh
  nvim.zsh
)
for f in $rcfiles
do
  source $HOME/dotfiles/zshfiles/rcfiles/$f
done

# Plugins {{{
eval "$(fasd --init auto)"
eval "$(direnv hook zsh)"
# }}}

# vim:foldmethod=marker
