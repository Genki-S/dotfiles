set background=dark
colorscheme solarized

" override some solarized colors below

" similar but different color for sign column (so that it looks like a padding, when no sign is shown)
highlight SignColumn ctermbg=0
